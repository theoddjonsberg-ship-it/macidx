# MACHINDEX_CORE_CONTEXT_BUILD_BRIEF.md

> Placeholder — full 27-section brief failed to upload via API (size + escaping).
> The canonical file lives locally at:
> `machindex-core/docs/MACHINDEX_CORE_CONTEXT_BUILD_BRIEF.md` (15 KB).
>
> To restore the full version, either:
> 1. Upload it via the GitHub web UI (drag & drop), or
> 2. Pull this branch locally and replace this file with the original.
>
> Step 1 implementation lives in `supabase/migrations/` and is unaffected.
> See `CLAUDE.md` and `docs/PROJECT_CONTEXT.md` for project scope.
